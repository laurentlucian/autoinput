// This file is kept for backward compatibility but is no longer the entry point.
// The app now uses TanStack Router — see src/lib/router.tsx and src/routes/
export default function App() {
  return null;
}
